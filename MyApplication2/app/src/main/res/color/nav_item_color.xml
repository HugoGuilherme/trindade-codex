<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Quando selecionado = amarelo (usa a cor que você definiu em colors.xml) -->
    <item android:color="@color/yellowAccent" android:state_checked="true"/>
    <!-- Quando não selecionado = cor padrão do texto sobre fundo -->
    <item android:color="?attr/colorOnBackground"/>
</selector>

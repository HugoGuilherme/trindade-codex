<?xml version="1.0" encoding="utf-8"?>
<androidx.drawerlayout.widget.DrawerLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/drawer_layout"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical">

        <!-- Toolbar -->
        <androidx.appcompat.widget.Toolbar
            android:id="@+id/toolbar"
            android:layout_width="match_parent"
            android:layout_height="?attr/actionBarSize"
            android:background="#04214D"
            android:theme="@style/ThemeOverlay.AppCompat.Dark.ActionBar"
            android:title="Meu Perfil"
            android:textAlignment="center" />

        <!-- Foto do Perfil -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:gravity="center"
            android:padding="16dp">

            <ImageView
                android:id="@+id/profile_image"
                android:layout_width="120dp"
                android:layout_height="120dp"
                android:scaleType="centerCrop"
                android:src="@drawable/ic_profile_placeholder"
                android:background="@drawable/circle_background"
                android:layout_marginBottom="8dp" />

            <TextView
                android:id="@+id/tv_change_photo"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Alterar Foto"
                android:textSize="14sp"
                android:textColor="@android:color/holo_blue_dark"
                android:padding="4dp"/>
        </LinearLayout>

        <!-- Conteúdo -->
        <ScrollView
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:padding="16dp">

            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="vertical">

                <TextView android:text="Nome:" style="@style/ProfileLabel"/>
                <TextView android:id="@+id/tv_username" android:text="Hugo Guilherme" style="@style/ProfileText"/>

                <View style="@style/ProfileDivider"/>

                <TextView android:text="Email:" style="@style/ProfileLabel"/>
                <TextView android:id="@+id/tv_email" android:text="hugo@email.com" style="@style/ProfileText"/>

                <View style="@style/ProfileDivider"/>

                <TextView android:text="Telefone:" style="@style/ProfileLabel"/>
                <TextView android:id="@+id/tv_phone" android:text="(21) 99999-9999" style="@style/ProfileText"/>

                <View style="@style/ProfileDivider"/>

                <TextView android:text="Gênero:" style="@style/ProfileLabel"/>
                <TextView android:id="@+id/tv_gender" android:text="Masculino" style="@style/ProfileText"/>

                <View style="@style/ProfileDivider"/>

                <TextView android:text="Data de Nascimento:" style="@style/ProfileLabel"/>
                <TextView android:id="@+id/tv_dob" android:text="10/04/1995" style="@style/ProfileText"/>

            </LinearLayout>
        </ScrollView>
    </LinearLayout>

    <!-- NavigationView (Menu Lateral) -->
    <com.google.android.material.navigation.NavigationView
        android:id="@+id/navigation_view"
        android:layout_width="wrap_content"
        android:layout_height="match_parent"
        android:layout_gravity="start"
        app:menu="@menu/menu_drawer"
        app:headerLayout="@layout/nav_header" />

</androidx.drawerlayout.widget.DrawerLayout>
